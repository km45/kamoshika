#!/bin/bash

function lint_config() {
  yamllint sample.yml
}

function lint_scripts() {
  pipenv run lint
  pipenv run mypy --ignore-missing-imports kamoshika
  pipenv run mypy tests
}

function main() {
  lint_config
  lint_scripts
}

main "$@"
